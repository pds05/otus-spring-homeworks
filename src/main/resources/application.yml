spring:
  data:
    mongodb:
      database: prod_db
      port: 27017
      host: localhost
  shell:
    command:
      stacktrace:
        enabled: true
mongock:
#  runner-type: "ApplicationRunner"
  runner-type: "InitializingBean"
  migration-scan-package: "ru.otus.hw.mongock.changeunits.prod"
  enabled: true
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG